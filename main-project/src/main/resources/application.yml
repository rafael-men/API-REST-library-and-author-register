server:
  port: 8000

spring:
  application:
    name:
  datasource:
    url: jdbc:postgresql://localhost:5432/librarydb
    username: postgres
    password: senha
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
